local constants = require "scripts.constants"

data:extend {
  {
    type = "font",
    name = constants.FONT_NAME,
    from = "default-bold",
    size = 13,
    border = true,
    border_color = {}
  }
}
